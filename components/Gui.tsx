import { useEffect, useRef } from 'react'
import styles from '../styles/Breakout.module.css'

export default function Gui({ options = undefined }) {
  const mockOptions = {
    test: 10,
    test2: 'TEST'
  }
  const optionsArray = Object.entries(mockOptions);

  return (
    <div>

    </div>
  )
}
